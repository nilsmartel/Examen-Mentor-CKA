# CKA Study Progress

> The mentor (Kube) maintains this file. It is the single source of truth for where the learner is.
> Status: `⬜ Not started` · `🟡 In progress` · `✅ Mastered` (only ✅ counts toward completion).
> Overall % is **weighted by exam domain** — see the formula at the bottom.

## 📊 Overall: ~43% complete  *(weighted by exam domain — see formula at bottom)*

`█████████░░░░░░░░░░░` 11 / 27 lessons mastered · the bar tracks **weighted %**, not the raw lesson count

> ▶️ **NEXT SESSION — start here:** **5.4 mastered — Troubleshooting is now 3/5 and we crossed 40%.** Next up:
> **5.3 resource-usage monitoring** (`kubectl top`, metrics-server) — it's short, and it's the **hard prerequisite for
> 3.3 HPA**, so plan to run **5.3 → 3.3 back-to-back** (that pairing also satisfies interleaving: 05 → 03).
> Warm-up should include: **kubeadm upgrade order** (partial 2026-08-27, due 08-30 — he now owns 1–3 + unhold/re-hold
> but framed drain as *conditional* and reached for `enable` instead of `daemon-reload`; **ask for steps 4–7 specifically**)
> and the **etcdctl 3 certs** (due 08-30 — **root cause finally found & fixed, see queue**; verify it stuck).
> Cluster is **2-node & both Ready v1.35.1**; `dbg` namespace deleted at session end. No env work needed.
> ⚠️ **Pods scheduled on `minikube-m02` get their dead-container logs GC'd fast** (docker driver) — if a future lab needs
> `--previous`, tell him to run it immediately or the file is gone. This bit us live today (and taught well).

**Strong:** **Troubleshooting (3/5 — the 30% domain is now the strongest by coverage)**, Cluster Architecture (4/8: RBAC + etcd + lifecycle/drain + kubeadm/bootstrap), Workloads (2/5), Services & Networking (2/6) · **Weak / next up:** **Storage is now the only untouched domain (0/3, 10%)** — schedule 4.1/4.2 soon so nothing is a total blank. Troubleshooting has 2 left (5.3 → 5.5). Services & Networking 4 to go (2.3–2.6).

---

## Learner context

- Experience at start: comfortable with pods, deployments, replicasets, services. New to
  cluster admin & troubleshooting depth.
- Background: holds a (now ~3-years-stale) CKAD. First-session diagnostic (2026-08-04) showed
  solid conceptual foundations across rollouts, ConfigMaps/Secrets, Services, and basic RBAC —
  rusty on exact command syntax rather than concepts. Good troubleshooting instincts once
  prompted Socratically. Can move faster through foundation topics than a true beginner; skip
  ground-zero explanations, focus on operational verbs, exact syntax, and exam traps.
- Pacing: **adaptive** (mentor chooses next topic).
- Target exam date: _not set_ (ask the learner; if set, note it here and bias pacing).
- Environment: minikube + podman on macOS (see `reference/environment-setup.md`).

---

## Domain 01 — Cluster Architecture, Installation & Config (weight 25%, 8 lessons)

| Lesson | Status | Last touched | Notes |
|--------|:------:|:------------:|-------|
| 1.1 RBAC | ✅ | 2026-08-07 | **Mastered.** Finished the namespaced-vs-cluster probe (nailed: cluster-wide pod read = ClusterRole+ClusterRoleBinding; RoleBinding→ClusterRole limits to one ns; two RoleBindings for dev+prod vs ClusterRoleBinding for all). Lab 1.1 A–D all hands-on: User/Role/RoleBinding, ServiceAccount + real in-pod token test (exec'd kubectl in a pod running as the SA — saw the 403 for `default`), ClusterRole-reused-per-ns. Break/fix: wrong-apiGroup (edited role apiGroups→`[""]`, watched deployments read silently break, read the 403 that names `apps`, fixed). Great curiosity: asked why apiGroups exist (taught groups/versioning/extensibility) + verb list. Confidence 4/5. |
| 1.2 kubeadm install & infra | ✅ | 2026-08-26 | **Mastered** (4th Domain 01 win) — the 3-session cliffhanger is CLOSED. Per the plan I **taught the bootstrap forward instead of re-asking a 4th time**, and it worked. Landed both trust directions: **(a) cluster→node** = the bootstrap token as a 24h Secret in kube-system, group `system:bootstrappers:kubeadm:default-node-token`, whose entire RBAC power is *create a CSR* (this was the half he was missing on 08-15) → CSR → auto-approve → `/var/lib/kubelet/pki/` → `kubelet.conf` → identity `system:node:<name>` scoped by the Node authorizer. **(b) node→cluster** = `--discovery-token-ca-cert-hash`. **He derived the hash mechanism unaided** (one-way fn, fingerprint, compare) but got the *motivation* wrong — said it was to avoid 'giving the cert away'. Corrected: **the CA cert is PUBLIC; the hash solves TRANSPORT, not secrecy** (2KB PEM can't travel out-of-band, 64 hex chars can; node pulls the CA anonymously from the `cluster-info` ConfigMap in kube-public). Anchored on the **SSH host-key fingerprint** analogy — that landed hard. Lab A+C hands-on. Self-test 2.5/3. Also fielded a genuinely sharp unprompted question — *'isn't `kubeadm upgrade apply` already updating the control-plane services?'* → gave the **cluster-scoped-once vs per-machine-every-node** split, which is the cleanest framing of the upgrade sequence he's had. ~34%→~37%. |
| 1.3 Cluster lifecycle & upgrades | ✅ | 2026-08-14 | **Mastered.** Part A drain lab done COLD on the live 2-node cluster: predicted cordon (existing pods stay, no NEW pods, incl. the nuance "if deleted they'd reschedule elsewhere"), drain (all 4 `web` pods evacuated to minikube, only kube-proxy DS left on m02), uncordon (correctly said pods do NOT auto-flow back — "that'd be an interrupt"; taught the no-built-in-rebalancer + optional descheduler aside). **Empirically tested drain without `--delete-emptydir-data` and it worked** → led to the unifying model: **drain is conservative; each flag is a WAIVER you sign** — `--ignore-daemonsets` (DS pods pinned per-node → eviction is a pointless loop) / `--delete-emptydir-data` (emptyDir = node-local scratch → eviction DESTROYS it). Corrected his DaemonSet-vs-Deployment framing (DS = one-per-node pinned vs Deploy = N-replicas-anywhere-movable). Part B upgrade order: got the middle+end, learned the front (kubeadm binary FIRST → `plan` preview → `apply`). **Key correction absorbed:** he thought the kubelet "patches itself" — taught the TWO-mechanism split (kubeadm rewrites CP static-pod manifests; kubelet+kubectl = OS packages via apt/yum + `systemctl restart`). Already owned apply-vs-node from 2026-08-13. **Open thread he requested:** deep-dive next session on step 5 (kubelet package upgrade + bootstrapping — pairs with 1.2). Env note: I repaired m02 CNI (was NotReady, `cni config uninitialized`, missing `1-k8s.conflist`). ~31%→~34%. |
| 1.4 HA control plane | ⬜ | — | |
| 1.5 etcd backup & restore | ✅ | 2026-08-10 | **Mastered.** Opened with a full certs/mTLS primer (built it Socratically: key pair → sign-with-private/verify-with-public → the trust gap → CA-signed cert binds pubkey↔identity → mTLS two-way → 3 etcdctl flags map onto the handshake). Learner derived the sign/verify trick, the "stolen photocopy needs the private key" impersonation point, and the ca.crt/crt-public vs key-secret split himself. Lab full A–D hands-on: snapshot save (verified w/ etcdutl status), created after-backup post-snapshot, restore into new dir, repointed manifest hostPath (nailed the "change ONLY the etcd-data hostPath, not --data-dir/mountPath" trap), watched after-backup vanish. **Standout insights:** asked what restore *does* (snap.db=archive vs restored/=bootable data-dir); asked about orphaned containers → led to the reconcile-loop explanation (kubelet GCs orphaned pods; restore rewinds desired state, controllers drag actual→desired). Grasped save-needs-3-certs (live mTLS) vs restore-needs-none (offline file op). **Minikube wrinkle handled:** host has no etcdctl + distroless image → drove via `kubectl exec etcd-minikube`, writing only to mounted paths. Confidence high. ~16%→~19%. |
| 1.6 Helm & Kustomize | ⬜ | — | |
| 1.7 Extension interfaces (CNI/CSI/CRI) | ⬜ | — | |
| 1.8 CRDs & operators | ⬜ | — | |

## Domain 02 — Services & Networking (weight 20%, 6 lessons)

| Lesson | Status | Last touched | Notes |
|--------|:------:|:------------:|-------|
| 2.1 Pod connectivity & network model | ✅ | 2026-08-05 | Proved all 4 model rules hands-on (distinct pod IPs, cross-pod curl by IP, ephemeral IP on recreate, shared-netns via localhost). Break-it/fix-it: two-nginx port-80 collision → "Address already in use" → split into separate pods. Self-tests passed; wobbles: said "CDI" for CNI (recapped), and didn't know Service ClusterIP is virtual/kube-proxy-translated (clarified). Confidence 3/5. |
| 2.2 Services & endpoints | ✅ | 2026-08-06 | Derived the whole model well. Nailed the chain (Service→selector→Ready pods→EndpointSlice→kube-proxy→pod) and both failure modes: empty ep = selector/readiness; populated-but-refused = wrong targetPort (verified hands-on via patch to 8080). Labs A–D done incl. two break/fix. Strong tangents self-driven: netns/port-sharing recap, EndpointSlice naming (<svc>-rand, find by label), port/targetPort/nodePort trichotomy, headless (clusterIP:None) vs hardcoded-IP, and derived the operator/label read-write-split pattern unprompted (previews 1.8). Brief StatefulSet intro given. Confidence 4/5. |
| 2.3 Ingress controllers & resources | ⬜ | — | |
| 2.4 Gateway API | ⬜ | — | |
| 2.5 Network Policies | ⬜ | — | |
| 2.6 CoreDNS | ⬜ | — | |

## Domain 03 — Workloads & Scheduling (weight 15%, 5 lessons)

| Lesson | Status | Last touched | Notes |
|--------|:------:|:------------:|-------|
| 3.1 Deployments, rollouts & rollbacks | ✅ | 2026-08-04 | Diagnostic showed strong prior knowledge; lab (create→update→break→undo→strategy knobs→restart) completed clean. Confidence self-rated 3/5 — re-quiz syntax details. |
| 3.2 ConfigMaps & Secrets | ✅ | 2026-08-05 | Concepts solid immediately (base64≠encryption, env-vs-mount update behavior predicted correctly). Lab parts A-C hands-on (create, env injection, mounted files); part D update-behavior verified by mentor at student's request. Syntax slips en route: invented `--from-label` flag, missing required `name:` on an `env:` entry. Confidence 3/5. |
| 3.3 Autoscaling (HPA) | ⬜ | — | |
| 3.4 Self-healing primitives | ⬜ | — | |
| 3.5 Scheduling (affinity, taints, limits) | ⬜ | — | |

## Domain 04 — Storage (weight 10%, 3 lessons)

| Lesson | Status | Last touched | Notes |
|--------|:------:|:------------:|-------|
| 4.1 Volumes, access modes, reclaim | ⬜ | — | |
| 4.2 PV & PVC | ⬜ | — | |
| 4.3 StorageClasses & dynamic provisioning | ⬜ | — | |

## Domain 05 — Troubleshooting (weight 30%, 5 lessons)

| Lesson | Status | Last touched | Notes |
|--------|:------:|:------------:|-------|
| 5.1 Cluster & node troubleshooting | ✅ | 2026-08-12 | **Mastered** (2nd Troubleshooting win). Taught the debug staircase (get nodes/pods → describe → logs → ssh+journalctl) + the polarity trap (Pressure conditions healthy=False, Ready healthy=True) + heartbeat-freeze (kubelet dies → LastHeartbeatTime freezes → conditions flip to **Unknown** not False after ~40s). Lab full: **Part C** mentor stopped kubelet → node NotReady, diagnosed cold (pods fine, node NotReady, all conditions Unknown → kubelet not responding), read `journalctl`, fixed with `systemctl enable --now`. Bumped into active-vs-enabled-vs-preset (taught: active/inactive=running now [start/stop]; enabled/disabled=on boot [enable/disable]; preset=distro default). **Part E** mentor corrupted `/var/lib/kubelet/config.yaml` (appended bad YAML) → kubelet crash-loops → **initially misread the error as "file not found"** (corrected: file present, contents malformed — read the FULL line `yaml: line 57: did not find expected node content`), also floated "restart kubeadm" (corrected: kubeadm is a one-shot CLI, not a service, doesn't repopulate). Fixed the file → self-healed via `Restart=always` before he even typed restart (taught: don't rely on it; explicit restart, daemon-reload only for unit-file edits). Closed with cordon (SchedulingDisabled/spec.unschedulable, no NEW pods) vs drain (cordon+evict, --ignore-daemonsets/--delete-emptydir-data) vs uncordon. Slips: `sysctl` vs `systemctl` typo; parse-vs-missing misread. Confidence 3/5 (modest — drove both breaks solo). ~25%→~31%. |
| 5.2 Control-plane components | ✅ | 2026-08-11 | **Mastered** (first Troubleshooting win — the 30% domain opens). Taught the static-pod model Socratically: killed the "scheduler controls the nodes" misconception with the bootstrap chicken-and-egg (kubelet runs `/etc/kubernetes/manifests/` FILES directly, bypassing scheduler+apiserver) → derived exam-trap #1 (can't `kubectl edit` a static pod, edit the file, kubelet recreates). Lab A–C full hands-on: **Part B** mystery scheduler break (bad image tag → ErrImagePull → reproduced "new pods stuck Pending" → fixed by reading the correct tag from another manifest, verified testpod schedules onto minikube). **Part C** the classic apiserver break (I seeded bad `--etcd-servers` port 9999) → kubectl died → diagnosed ENTIRELY on the node: `crictl ps -a` (taught why `-a`: evidence is in the crashed corpse) + `crictl logs` → read the fatal `context deadline exceeded` / etcd `connection refused` → found correct port 2379 by reading etcd.yaml (didn't guess) → fixed manifest → kubelet recreated → cluster back. **Standout:** independently spotted storage-provisioner CrashLoopBackOff and, once he saw it self-heal, correctly classified it as *aftermath* not *root cause* — the exact cause-vs-collateral discrimination the exam rewards. Also learned the post-recovery "wait 30–60s, node briefly NotReady is just heartbeat re-posting" patience lesson. Slip: ran `crictl` without sudo → go-panic logs (resolved: needs sudo). Confidence 3/5 (slightly under — mostly solo). ~19%→~25%. |
| 5.3 Resource-usage monitoring | ⬜ | — | |
| 5.4 Container output & logging | ✅ | 2026-08-27 | **Mastered** (3rd Troubleshooting win — domain now 3/5). Fast, high-quality session; he drove nearly all of it. Taught the **two-streams split** (`journalctl -u kubelet` = node agent's own output vs `kubectl logs` = your app's stdout — he initially conflated them, corrected in one nudge), then made `--previous` **mechanical rather than magic**: runtime writes `/var/log/pods/<ns>_<pod>_<uid>/<ctr>/N.log`, one file per restart; plain `logs` reads the newest (a 4-second-old container), `--previous` reads N-1. He connected it unprompted to `crictl ps -a` from 5.2 — *"evidence is in the corpse."* **Two unscripted wins:** (1) Part A's `--previous` **failed live** — `unable to retrieve container logs for docker://…`; I pulled the node's log dir and only `4.log` survived → he reasoned out dead-container GC himself, then re-ran fast and got the payoff. Generalized to the real exam rule: *`--previous` is one step back and it has a shelf life — run it immediately.* (2) Part C's OOM showed **`Reason: OOMKilled` + `Exit Code: 1`, not 137** → taught that `Reason` and `Exit Code` are **independent** (a child got OOM-killed, PID 1 exited with 1; kubelet reads the memory cgroup for `Reason`). Corrected my own earlier tidy framing in front of him. Also derived: **`-BackOff` suffix = kubelet's retry rate-limiter, never a root cause**. Lab A–D full hands-on incl. fixing badimage. Self-test **3/3** on scenario framing. Confidence **4/5** ("taxing, but a strong 4"). ~37%→~43%. |
| 5.5 Services & networking troubleshooting | ⬜ | — | |

---

## 🔁 Spaced-repetition queue

Items to re-quiz, with next review date. Add an item when a lesson hits ✅.
On success push the interval out (~2d → ~5d → ~10d); on a miss reset to ~1d.

| Item / concept | Lesson | Last reviewed | Next review | Interval |
|----------------|:------:|:-------------:|:-----------:|:--------:|
| `rollout undo --to-revision` is absolute, not relative; undo re-numbers the promoted RS to the new highest revision | 3.1 | 2026-08-10 | 2026-08-20 | 10d |
| Editing a live pod doesn't self-heal instantly — RS reconciles pod *count* not spec; reverts only on pod *replacement* | 3.1 | 2026-08-08 | 2026-08-13 | 5d |
| _(retired 2026-08-11 — owned)_ env vars captured at pod start (need `rollout restart`) vs mounted volumes update in place. Learner asked to stop quizzing; nailed 4×. | 3.2 | 2026-08-11 | — | retired |
| No CNI → nodes `NotReady`; CNI broken later → pods `ContainerCreating` (plugin present but can't wire the sandbox/give an IP; `describe pod` shows the CNI sandbox error). Fix = apply/repair CNI manifest, not restart apiserver/kubelet. **Lifecycle→status map: stuck before scheduling=`Pending`; pulling=`ErrImagePull`; wiring network=`ContainerCreating`.** NEW ANCHOR: `describe pod` → **`Node:` empty = scheduler's problem / assigned = kubelet's problem** | 2.1 | 2026-08-26 | 2026-08-31 | 5d (**✅ OWNED at last — 5th exposure, first success.** Scenario framing was the unlock: he answered 'scheduled already → CNI plugin → `describe pod`' instantly. **Keep asking it as a scenario, never as a definition.** Prior note: **4th exposure, still not owned.** Knew it was scheduled + past pull, but couldn't name the cause and didn't produce `Pending` for the not-scheduled case. Asked for a cheatsheet → gave the full stuck-status→cause table + the `Node:`-field discriminator, now in cheat-sheet.md. **Next time quiz it as a scenario, not a definition** — "pod stuck ContainerCreating 5 min, what's your first command and top suspect?") |
| **Stuck-status triage (full table, see cheat-sheet.md):** `CreateContainerConfigError`=missing ConfigMap/Secret · `CrashLoopBackOff`=app crash→`logs --previous` · `Running 0/1`=readiness probe failing (the empty-endpoints cause) · `OOMKilled`=over memory limit · stuck `Terminating`=finalizer/node gone | 5.1 | 2026-08-27 | 2026-09-01 | 5d (✅ effectively re-quizzed across the whole 5.4 lab — he produced the right tool for CrashLoop / ImagePull / OOM / Pending unaided) |
| **Evidence-location map (5.4 core):** `Pending`→**events** (`Node:` empty ⇒ no container ever existed ⇒ logs are *impossible*, not just unhelpful) · `ImagePullBackOff`→**events** (never ran a line of your code — NOT an app bug) · `CrashLoopBackOff`→**`logs --previous`** (the only one that IS an app bug) · `OOMKilled`→**`describe`→Last State** · Running-but-broken→logs+exec+probes. `kubectl get events` is **unsorted by default** → `--sort-by=.lastTimestamp` | 5.4 | 2026-08-27 | 2026-08-29 | 2d (new; 3/3 on the self-test) |
| **`--previous` is mechanical, not magic:** runtime writes `/var/log/pods/<ns>_<pod>_<uid>/<ctr>/0.log,1.log,2.log…` — **one file per restart attempt**. Plain `logs` reads the newest (a 4-second-old container that hasn't errored yet); `--previous` reads **N-1**. The kubelet **GCs dead containers** → you get **ONE** step back and it has a **shelf life** ⇒ on a CrashLoopBackOff run `logs --previous` **immediately**, before `describe`. `"previous terminated container not found"` is an **ERROR, not empty output** = this pod has never restarted (itself diagnostic). | 5.4 | 2026-08-27 | 2026-08-29 | 2d (new; **learned the hard way — it failed live on m02 and he reasoned out the GC cause himself**) |
| **Two log streams, never conflate:** `journalctl -u kubelet` = the **node agent's own** output (mount failures, sandbox errors; needs ssh+sudo) vs `kubectl logs` = **your app's** stdout/stderr, **PID 1 only**. Corollary trap: an app that logs to a **file inside the container** is invisible to `kubectl logs` → `exec` in and read it. | 5.4 | 2026-08-27 | 2026-08-29 | 2d (new; he *did* conflate them on first ask — said journalctl would show container logs — corrected in one nudge) |
| **`-BackOff` suffix = the kubelet's retry RATE-LIMITER, never a root cause.** Strip it and ask what actually failed. `ErrImagePull`→`ImagePullBackOff` is one disease at two stages; `CrashLoopBackOff` = "container exits" + kubelet pacing. | 5.4 | 2026-08-27 | 2026-08-29 | 2d (new; he derived the ErrImagePull/ImagePullBackOff difference unaided) |
| **Exit codes are UNIX: killed by signal N ⇒ 128+N.** 137=128+9 SIGKILL · 143=128+15 SIGTERM · 139=segfault · 0=clean · 1=general error. **★ `Reason:` and `Exit Code:` are INDEPENDENT signals — trust `Reason: OOMKilled`** (authoritative, kubelet reads the memory **cgroup**); 137 is a strong hint, not a requirement (an OOM-killed **child** can leave PID 1 exiting with plain `1` — seen live in the lab). 137 + `OOMKilled` ⇒ raise `resources.limits.memory`; 137 during a delete ⇒ grace period (default **30s**) expired, normal. Bare-pod resources aren't editable in place → delete & recreate; Deployment → edit the pod **template**. | 5.4 | 2026-08-27 | 2026-08-29 | 2d (new; derived 137 and 143 from 128+N unaided, and got *both* routes to SIGKILL) |
| Service ClusterIP is virtual (no real interface); kube-proxy translates it to a pod IP. Pod IP is a real endpoint | 2.1 | 2026-08-06 | 2026-08-11 | 5d |
| Pod IPs are ephemeral (new IP on recreate) → never hardcode; use a Service for a stable VIP/DNS | 2.1 | 2026-08-06 | 2026-08-11 | 5d |
| Service debug chain: Service(selector)→Ready pods→EndpointSlice→kube-proxy→pod. `kubectl get ep` is first move | 2.2 | 2026-08-08 | 2026-08-13 | 5d |
| Empty endpoints = selector mismatch OR pods not Ready; populated endpoints but "refused" = wrong targetPort | 2.2 | 2026-08-08 | 2026-08-13 | 5d |
| 3 ports: port (svc, client-facing) / targetPort (pod's real listen port, must match app) / nodePort (external 3xxxx) | 2.2 | 2026-08-12 | 2026-08-17 | 5d |
| Headless Service (clusterIP:None) = no VIP; DNS returns pod IPs; +StatefulSet gives stable per-pod names (web-0.web…) | 2.2 | 2026-08-10 | 2026-08-15 | 5d |
| etcd `snapshot save` needs 3 certs (live mTLS: cacert verifies etcd, cert+key = your client identity); `snapshot restore` needs ZERO certs (offline file op, no endpoint). Restore uses `etcdutl`, save uses `etcdctl` | 1.5 | 2026-08-27 | 2026-08-30 | 3d (**★ ROOT CAUSE FOUND AND FIXED 2026-08-27 — after 3 sessions of mislabeling the 3rd flag.** 2/3 again at first (`--cacert` ✅, `--cert`=mine ✅, `--key`="etcd's private key" ❌), but this time he asked *"why would I ever give away my own private key?"* — which exposed **two** wrong premises: (a) he thought the flags describe *things handed over the wire* (they don't — `--key` tells the LOCAL client where to read the key; it never leaves disk), and (b) **he was collapsing `etcdctl` and `etcd` into one thing** — with no client/server distinction, "whose key?" has no answer. He got there himself: *"oh wait — this is my way of talking WITH etcd, and etcdctl != etcd."* Taught: cert+key are an inseparable **matched pair, same owner** (public claim + private proof); holding the server's private key would let you *be* etcd; **mTLS has no login — the certificate IS the login**. Re-verify next session; if it holds, push to 7d. Quiz as: "name what each flag IS, whose.") |
| etcd restore = 2 moves: (1) `etcdutl snapshot restore snap.db --data-dir=NEW` (offline), (2) repoint the etcd static-pod manifest's **etcd-data hostPath** to NEW (leave `--data-dir` flag + mountPath). kubelet restarts etcd | 1.5 | 2026-08-12 | 2026-08-17 | 5d |
| AFTER an etcd restore, restart kube-controller-manager + kube-scheduler — their watch caches hold future resourceVersions the rewound etcd no longer has → stalls (e.g. namespace stuck Terminating). apiserver self-restarts. Restart a static pod by moving its manifest out of /etc/kubernetes/manifests and back | 1.5 | 2026-08-12 | 2026-08-17 | 5d (✅ nailed both components + the watch-cache reason) |
| _(retired 2026-08-15 at learner's request)_ Locate static-pod manifests: `/etc/kubernetes/manifests/` (**/etc** not /var — it's config). Learner said `/var` again on 2026-08-15; retaught with the `/etc`=authored-config vs `/var/lib`=machine-state rule, then asked to mark it done — he's confident he'll find it fast in the exam. **Do not re-quiz this path, and don't push the `grep staticPodPath` derivation one-liner (he rejected it as more to memorize than the path itself).** | 1.5 | 2026-08-15 | — | retired |
| Role/RoleBinding are namespaced; cluster-scoped resources (nodes, PVs, namespaces) REQUIRE ClusterRole+ClusterRoleBinding — a Role can't grant them. The binding sets the scope, the role sets the powers | 1.1 | 2026-08-11 | 2026-08-16 | 5d |
| Static pods = kubelet runs manifests in `/etc/kubernetes/manifests/` FILES directly (bypasses scheduler+apiserver, so control plane can bootstrap). FIX = edit the file on the node → kubelet auto-recreates; `kubectl edit`/`apply` won't stick | 5.2 | 2026-08-11 | 2026-08-13 | 2d |
| API server DOWN → `kubectl` = connection refused → ssh to node. `sudo crictl ps -a` (`-a` shows the CRASHED container — evidence is in the corpse; needs sudo) + `sudo crictl logs <id>` for the fatal error | 5.2 | 2026-08-11 | 2026-08-13 | 2d |
| Symptom→component map: apiserver down = kubectl refused; scheduler down = new pods stuck **Pending** (nodes healthy, existing pods fine); controller-manager down = no self-heal/endpoints; etcd down = apiserver won't start (presents AS apiserver-down: etcd dead→apiserver dead→kubectl refused; tell = apiserver logs `context deadline exceeded`/etcd refused) | 5.2 | 2026-08-13 | 2026-08-18 | 5d (✅ sharpened the etcd-down→apiserver-down chain) |
| After recovering the control plane, wait 30–60s: a brief node `NotReady` + downstream CrashLoops (e.g. storage-provisioner) are **aftermath** (lost apiserver connection), not root cause. Fix the ONE real thing; collateral self-heals. Discrimination test = fix root cause, wait, re-check: clears on its own → collateral; persists → separate problem | 5.2 | 2026-08-13 | 2026-08-18 | 5d (✅ nailed aftermath; reinforced the fix-one-thing-then-recheck test) |
| `auth can-i` uses `--as` (impersonate); `--user` picks a kubeconfig entry (→ "does not exist"). can-i never errors (default-deny); the real request gives a 403 naming group/resource/ns/verb | 1.1 | 2026-08-12 | 2026-08-17 | 5d |
| Wrong apiGroup silently grants nothing: deployments=`apps`, pods/configmaps/nodes=core `""`. Imperative `create role --resource=` auto-resolves the group; hand-written YAML is where the trap bites | 1.1 | 2026-08-07 | 2026-08-09 | 2d |
| SA identity in bindings/impersonation = `system:serviceaccount:<ns>:<name>`; SAs are real namespaced objects (fully-qualified), Users/Groups are just trusted strings (no object). `edit` is NOT a verb → use `update`/`patch` | 1.1 | 2026-08-07 | 2026-08-09 | 2d |
| NotReady node = kubelet stopped posting heartbeat → after ~40s conditions flip to **Unknown** (not False — node-controller hears nothing). `LastHeartbeatTime` freezes. Fix lives on the NODE (systemd), not the API. kubelet is a **systemd service** → logs via `journalctl -u kubelet`, not crictl/kubectl | 5.1 | 2026-08-12 | 2026-08-14 | 2d |
| Two kubelet failure modes, told apart by `systemctl status`: **`inactive (dead)`** = just stopped → `systemctl start`. **`activating (auto-restart)`/`failed`** = crash-looping on bad config → READ `journalctl` (e.g. `yaml: line N` = parse error, file PRESENT not missing), fix `/var/lib/kubelet/config.yaml`, `restart`. `Restart=always` self-heals once fixed but don't wait; `daemon-reload` only after editing a unit FILE | 5.1 | 2026-08-12 | 2026-08-14 | 2d |
| systemd axes are independent: **active/inactive** (running now; `start`/`stop`) vs **enabled/disabled** (auto-start on boot; `enable`/`disable`); `preset` = distro default. `enable --now` = start + enable in one. cordon = `SchedulingDisabled` (spec.unschedulable, no NEW pods, existing stay) vs drain = cordon + EVICT (`--ignore-daemonsets --delete-emptydir-data`); `uncordon` to restore | 5.1 | 2026-08-12 | 2026-08-14 | 2d |
| drain is CONSERVATIVE — each flag is a waiver you sign: `--ignore-daemonsets` (DS pods pinned per-node → evicting is a pointless loop, recreated instantly) / `--delete-emptydir-data` (emptyDir = node-local scratch → eviction DESTROYS it). drain = cordon + evict (no separate cordon step). uncordon does NOT rebalance running pods back (no built-in rebalancer; descheduler is an optional add-on, not core/not on exam) | 1.3 | 2026-08-14 | 2026-08-16 | 2d (new; nailed all 3 predictions cold) |
| DaemonSet = one pod pinned to EVERY matching node (kube-proxy, CNI agents, log shippers); Deployment/ReplicaSet = N replicas *anywhere*, movable. That's why drain skips DS pods but reschedules Deploy pods | 1.3 | 2026-08-14 | 2026-08-16 | 2d (new) |
| Upgrading the kubelet = **replace the binary AND restart the process** — a new binary on disk does NOT upgrade the running kubelet (systemd is holding a process started from the OLD file). Hence `systemctl daemon-reload && systemctl restart kubelet`. `daemon-reload` is needed *because the kubeadm package edits the unit drop-in* `/etc/systemd/system/kubelet.service.d/10-kubeadm.conf` — same "only after a unit-FILE edit" rule from 5.1, the editor was just apt | 1.3 | 2026-08-15 | 2026-08-17 | 2d (new; ✅ he nailed both halves cold) |
| **Two independent guardrails against an accidental version jump:** (1) `pkgs.k8s.io` is **per-minor** (`core:/stable:/v1.35/deb/`) → a v1.34-configured box structurally *cannot* be offered 1.35; guards **magnitude**. (2) `apt-mark hold` → guards **timing/intent** (blocks patch drift 1.34.0→1.34.3 + the un-drained kubelet restart it causes). **Not redundant: `hold` is what makes routine `apt upgrade` for OS CVEs safe on a node.** Upgrade = edit repo file → unhold → install **exact** `=1.35.1-1.1` → re-hold | 1.3 | 2026-08-15 | 2026-08-17 | 2d (new; didn't know `apt-mark` at all, then asked the sharp question "doesn't the repo guardrail make hold redundant?" — got the two-axes answer) |
| `apt-get update` = refresh the package **index** (installs nothing, safe) vs `apt upgrade` = actually install newer versions of everything (**dangerous on a k8s node**). Debug `"version not found"` with **`apt-cache policy <pkg>`** → prints available versions + **which repo each came from**. Package name ≠ command name → `dpkg -S $(which <cmd>)` for the truth; `dpkg -L <pkg>` lists a package's files | 1.3 | 2026-08-15 | 2026-08-17 | 2d (new; he said "`apt upgrade` to fetch current packages" — right instinct, wrong+dangerous command) |
| kubeadm upgrade order (CP node): (1) upgrade **kubeadm binary** FIRST → (2) `kubeadm upgrade plan` (preview) → (3) `kubeadm upgrade apply vX.Y.Z` (rewrites CP static-pod manifests: apiserver/cm/scheduler/etcd — NOT kubelet) → (4) `drain` → (5) upgrade **kubelet+kubectl OS packages** → (6) `daemon-reload && restart kubelet` → (7) `uncordon`. Worker: same but step 3 = `kubeadm upgrade node` (reads CP's decision). **TWO mechanisms per node: kubeadm=CP pods; package manager=kubelet/kubectl binaries. Kubelet does NOT self-patch.** | 1.3 | 2026-08-27 | 2026-08-30 | 3d (**RECOVERED to a partial 2026-08-27** — big improvement on the hard miss 24h earlier. Cold, he produced **kubeadm binary → plan → apply → kubelet → restart** ✅ and volunteered the **`apt-mark unhold` → install exact version → re-hold** sandwich unprompted ✅. **Two gaps, both in the per-machine half:** (1) framed **drain as conditional** — *"if we schedule workers on the control-plane"* → corrected: drain the CP node **unconditionally**, it's ritual not judgment (cost of an extra drain = 0; cost of a skipped one = evicted-under-you workloads). He then supplied `uncordon` on one nudge. (2) For the missing systemd command he reached for **`enable`** (wrong axis — kubelet was already enabled) instead of **`daemon-reload`**; got it after one hint (*"the apt install rewrote `10-kubeadm.conf`; what does systemd believe?"*). **Next time ask specifically for steps 4–7**, the front is solid. Framing that keeps working: steps 1–3 = upgrade THE CLUSTER (once); 4–7 = upgrade THIS MACHINE (every node).) |
| **Bootstrap token = cluster trusts NODE.** A real Secret in `kube-system` named `bootstrap-token-<id>`, default TTL **24h**, group `system:bootstrappers:kubeadm:default-node-token` whose **entire RBAC power is "may create a CSR"** — not a skeleton key. TTL is just an `expiration` key in `data:`; **no key = never expires** (minikube does this via `--ttl 0`, which is how m02 joined 13d later). **Deleting the Secret revokes the token instantly.** | 1.2 | 2026-08-26 | 2026-08-28 | 2d (new) |
| **`--discovery-token-ca-cert-hash` = node trusts CLUSTER.** Why a hash: **the CA cert is PUBLIC — the problem is TRANSPORT, not secrecy.** A 2KB PEM can't travel out-of-band; 64 hex chars can. Node fetches the CA anonymously from the **`cluster-info` ConfigMap in `kube-public`** over the untrusted channel, hashes it, compares to the hand-carried hash → defeats a MITM serving its own CA. **Same pattern as an SSH host-key fingerprint.** | 1.2 | 2026-08-26 | 2026-08-28 | 2d (new; he derived the *mechanism* solo, got the *motivation* wrong — watch that the public-vs-secret point stuck) |
| kubeadm init order: **prep every box** (containerd + `cgroupDriver=systemd`, kubeadm/kubelet/kubectl, `swapoff -a`, `br_netfilter`+`bridge-nf-call-iptables=1`) → `kubeadm init --pod-network-cidr` → `cp admin.conf ~/.kube/config` → **★ apply a CNI ★** → `kubeadm join`. **kubeadm does NOT install a CNI** → without it, permanent `NotReady`. kubelet must already be RUNNING before init (init writes static-pod manifests for it to pick up). `--pod-network-cidr` must match what the CNI expects. | 1.2 | 2026-08-26 | 2026-08-28 | 2d (new; **he omitted the CNI step** in his recitation — the #1 listed exam trap — while correctly *diagnosing* missing-CNI in the very next question. Re-ask.) |
| Two kubelet files, don't conflate: **`/etc/kubernetes/kubelet.conf` = IDENTITY** (kubeconfig, `system:node:<name>`; broken → auth errors) vs **`/var/lib/kubelet/config.yaml` = BEHAVIOR** (staticPodPath, cgroupDriver, clusterDNS; broken → kubelet won't start — this is the file he corrupted in 5.1). Certs: **`/etc/kubernetes/pki/`** on real kubeadm. **Don't guess a path — grep the static-pod manifest** (`grep ca-file /etc/kubernetes/manifests/kube-apiserver.yaml`), same move that won him the 5.2 apiserver break. | 1.2 | 2026-08-26 | 2026-08-28 | 2d (new; he pointed at `/etc/kubernetes/*.conf` for the kubelet config → taught the identity-vs-behavior split) |
| Exam hooks for a broken join: **`kubectl get csr`** (a stuck join surfaces as a PENDING CSR) → **`kubectl certificate approve <csr>`**; lost/expired token → **`kubeadm token create --print-join-command`**; inspect with `kubeadm token list` (TTL/EXPIRES/EXTRA GROUPS). | 1.2 | 2026-08-26 | 2026-08-29 | 3d (new) |
| **kubeconfig = mTLS serialized to YAML** (same shape as the etcdctl flags and `kubelet.conf`): `clusters.certificate-authority-data` verifies the apiserver (one to check them) + `users.client-certificate-data`/`client-key-data` prove who you are (two to prove yourself) + `contexts` = cluster+user+namespace. kubectl reads `~/.kube/config` or `$KUBECONFIG`; **"connection refused to localhost:8080" = NO config found**, not a dead apiserver. **Exam hook: ssh'd onto a node you have no kubeconfig → `sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes`.** `admin.conf` draws power from an RBAC binding (mangled RBAC can lock it out); `super-admin.conf` (1.29+) is break-glass. | 1.2 | 2026-08-26 | 2026-08-28 | 2d (new — **he flagged the admin.conf copy as entirely new to him**; taught at session close. Connects cleanly to 1.5's mTLS model and the 1.1 RBAC binding model, so quiz it as "where have you seen this shape before?") |

## ⚠️ Weak spots

_(Mentor: log specific misconceptions or repeated errors here, e.g. "confuses NodePort range",
"forgets to switch context". Target these with deliberate practice.)_

- **Pending vs. image-pull failures (2026-08-04):** initially reached for "check image pull" on a
  `Pending` pod before scheduling. Self-corrected well once prompted Socratically (scheduler must
  place the pod before kubelet can pull anything). Reinforce during 5.1/3.5 — this is a classic
  exam trap when skimming `kubectl get pods` output.
- **Manifest field slips (2026-08-05):** invented a nonexistent `--from-label` flag (conflating CLI
  literals with label metadata) and omitted the required `name:` on an `env:` entry (gave
  `valueFrom` with no name). Both self-corrected with one nudge. Not a conceptual gap — watch for
  more "forgot a required field" slips under exam time pressure; a quick `kubectl explain` habit
  would catch these.
- **RBAC: SA vs User conflation (2026-08-06→RESOLVED 2026-08-07):** previously thought humans auth *as*
  SAs. In the 1.1 lab he articulated the split cleanly (Users = trusted strings/no object; SAs = real
  namespaced objects, `system:serviceaccount:ns:name`) and verified it via a real in-pod token test.
  Considered resolved — keep the recall item live.
- **`--as` vs `--user` on `auth can-i` (2026-08-07):** used `--user=jane` → "jane does not exist".
  `--user` selects a *kubeconfig* entry; impersonation is `--as`. Quick slip, corrected; watch under time
  pressure. Also flagged: `edit` is not an RBAC verb (→ `update`/`patch`).
- **Certificates / PKI gap (2026-08-08 → RESOLVED 2026-08-10):** did the full primer to open the session.
  Learner built the model himself Socratically — sign-with-private/verify-with-public, the trust gap,
  CA-signed cert binds pubkey↔identity, mTLS two-way, and the "stolen photocopy can't sign without the
  private key" impersonation point. Cleanly stated ca.crt/crt = public-shareable vs key = secret-revoke.
  One residual slip to watch: under quiz he mislabeled the 3 etcdctl flags as "etcd's key + apiserver's
  pubkey" instead of CA cert + *client's own* cert + *client's own* key (the lab's server-cert-reused-as-
  client-cert shortcut probably confused him). Recall item covers it. Foundation now solid for 1.2/5.2.
- **`crictl` needs `sudo` (2026-08-11):** ran `crictl` un-privileged during 5.2 → got a Go panic/connection stack trace, briefly thought crictl was broken. It's just root-only (runtime socket). On the node, always `sudo crictl …`. Low stakes, one-time nudge — flag if it recurs when the API server is down and crictl is the *only* tool.
- **CNI naming (recurring):** said "CDI" for CNI again (3rd time, from 2.1). Diagnosis/reasoning is
  correct every time — purely the acronym. Had him say "CNI" back. Low stakes but worth a nudge if it recurs.
- **Error-message misread: "not found" vs "malformed" (2026-08-12, 5.1):** on the corrupt-kubelet-config
  break he read `failed to load kubelet config file` as "no file found" and proposed regenerating via kubeadm,
  when the file was present and only its *contents* were bad (`yaml: line 57`). Corrected by making him paste the
  FULL journalctl line. **Exam-relevant pattern:** slow down and read the whole error — "load failed / parse error"
  (fix the contents) is a different bug from "not found" (regenerate). Watch for this under time pressure.
- **`sysctl` vs `systemctl` (2026-08-12):** typo'd `sysctl` (kernel tunables) for `systemctl` (service manager).
  One-time; flagged for exam-speed muscle memory. Also `/var/kubernetes` vs correct `/etc/kubernetes` on recall.
- **✅ `ContainerCreating` — RESOLVED 2026-08-26 after 5 exposures (was the most persistent gap).** Every *definitional*
  quiz failed (2.1 lab, 08-12, 08-14, 08-15). On 08-26 I asked it as a **scenario** — "pod stuck ContainerCreating 5 min,
  first command, top suspect?" — and he answered instantly and completely: *"scheduled on a node already… often points to
  the CNI plugin being down… `kubectl describe pod`."* **The lesson generalizes beyond this item: he reasons excellently
  from symptoms and poorly from vocabulary. Frame recall as scenarios, not definitions.** Keep the item alive at long
  intervals; it's owned, not yet automatic.
- **`apt-get update` vs `apt upgrade` (2026-08-15):** answered "sudo apt upgrade to fetch current packages" —
  correct *instinct* (refresh what's available), wrong and actively dangerous command on a k8s node (`upgrade`
  installs, `update` only refreshes the index). Corrected. Low exam stakes directly, but it's the same
  read-the-verb-carefully failure mode as the "not found vs malformed" misread from 5.1 — worth watching as a pattern.
- **Tangent-driven pacing (2026-08-15, not a deficiency — a working note):** he interrupts the planned thread with
  deep "why does this exist" questions (asked for a full apt/repo tutorial mid-lesson, then the sharp
  "doesn't the per-minor repo make `hold` redundant?"). These are *high-value* — he retains what he digs into, and
  the guardrail question was the best question of the session. But it cost the whole bootstrap half: the same
  question went unanswered 3× because each ask got tangented. **Tactic:** answer the tangent, but keep the open
  question visible and prefer *teaching it forward* over re-asking a 4th time.
- **`kubeadm` vs `kubelet` naming slip (2026-08-26, NEW):** in the final recitation wrote *"kubelet init"* and *"kubelet join"*
  for `kubeadm init`/`kubeadm join`. Second instance of this family — in the 5.1 lab he proposed "restart kubeadm" (corrected then:
  kubeadm is a one-shot CLI, not a service). The two names are one letter apart and do completely different jobs
  (**kubeadm = one-shot bootstrap CLI; kubelet = the long-running per-node daemon**). Cheap to fix, but it's the kind of slip
  that costs real minutes when a command silently doesn't exist under exam pressure.
- **"CDI" for CNI — 4th occurrence (2026-08-26):** said it again while *correctly* diagnosing the NotReady-after-init question.
  Purely the acronym; the reasoning is right every single time. Had him say "CNI" back. Note 1.7 (Extension interfaces) is still
  `⬜` — he fairly pointed out he's only ever met CNI as a symptom, never as a subject. **That's the real fix: teach 1.7.**
- **Compressing prerequisite lists under recall (2026-08-26):** his init sequence collapsed stage 1 to "install kubelet+kubeadm",
  dropping the container runtime, `swapoff`, and the sysctl/br_netfilter prep — and he placed "start kubelet daemon" *after* init
  rather than as a precondition. Concepts are fine; it's list-completeness that erodes. Same shape as the missing-CNI omission.
  Worth one timed "name all five prep items" drill before the exam.
- **✅ etcdctl 3-cert mislabel — RESOLVED 2026-08-27 after 3 failures (root cause found).** The recurring wrong answer for
  `--key` ("etcd's private key" / "public key of kube api") was never a memory problem. Two bad premises underneath it:
  (a) he read the flags as *things handed over the wire*, so "hand over my private key" felt absurd and his brain reassigned
  the flag; (b) **he was collapsing `etcdctl` and `etcd` into a single entity** — with no client/server split, "whose key?"
  is an unanswerable question. He surfaced it himself with a great question (*"why would I ever give away my own private key?"*)
  and closed it himself (*"oh wait — this is my way of talking WITH etcd, and etcdctl != etcd"*). **Generalizable lesson for me:
  when an item misses 3×, stop re-teaching the fact and go hunting for the broken premise underneath it.** That's what finally worked.
- **`kubectl log` (singular) — 3× in one session (2026-08-27, NEW):** wrote `k log pod` repeatedly; the command is `kubectl logs`.
  Same family as the **`kubeadm` vs `kubelet`** slips: a name that's *nearly* right and silently doesn't exist. Zero conceptual
  weight, real cost under the clock. Nudged three times; watch whether it persists into 5.3/5.5.
- **Loose crypto vocabulary (2026-08-27, minor):** described signature verification as etcd "decrypting" his signed response.
  The mechanism he described was correct; the verb was wrong. Nudged toward saying **sign / verify** as a fixed pair. Only
  matters if it signals fuzziness later — his actual model is sound.
- **Skips the prediction step when he's confident (2026-08-27, working note):** twice today I asked him to *predict before running*
  (Part D: "which tools will be useless here?") and he simply ran the command and reported the answer. He was right both times, so
  it costs nothing on accuracy — but prediction-before-execution is where the retrieval benefit lives, and the exam rewards
  choosing the right command *first*. Consider phrasing it as "don't run anything yet —" to force the beat.


---

## 🗒️ Session log

| Date | Topics covered | Labs done | Outcome / notes |
|------|----------------|-----------|-----------------|
| 2026-08-04 | First-session diagnostic (rollouts, ConfigMaps/Secrets, Services, RBAC, Pending-vs-troubleshooting) + 3.1 Deployments/rollouts | lab-3.1-rollouts (full: update, break, undo, strategy knobs, restart) | 3.1 mastered ✅. Strong foundation confirmed across the board; syntax rusty but concepts solid. Confidence 3/5 — queued for 2-day recall. |
| 2026-08-05 | Recall warm-up (3.1: rollback revision, live-pod edit) + 3.2 ConfigMaps & Secrets | lab-3.2-config-secrets (A-C hands-on; D update-behavior mentor-verified) | 3.2 mastered ✅. Base64≠encryption and env-vs-mount update behavior both predicted correctly pre-lab. Minor manifest-field slips, self-corrected. Confidence 3/5 — queued for 2-day recall. |
| 2026-08-05 | Ad-hoc recall (3.1 rollout cmds, 3.2 env-var staleness) + 2.1 Pod connectivity & network model (CIDR/NAT/flat-network explained from scratch) | lab-2.1-pod-connectivity (A cross-pod curl by IP, B ephemeral IPs, C shared netns; break-it/fix-it: two-nginx port-80 collision) | 2.1 mastered ✅. Productive struggle — initially doubted pods have IPs, disproved own hypothesis via `get pods -o wide`. Needed CNI taught directly (didn't know the component). Wobbles: "CDI" for CNI; unaware Service IP is virtual. Confidence 3/5. |
| 2026-08-06 | Recall (3.1 live-pod edit, 2.1 ClusterIP-virtual + ephemeral IPs — all solid) + 2.2 Services & Endpoints. Foundational Q first: how many pods share a node/IPs → network namespaces & per-namespace port space (tied back to 2.1 shared-netns lab). | lab-2.2-services A–D: ClusterIP + wget-by-DNS, break selector→empty ep→fix, NodePort + 3-port trichotomy, targetPort→8080 mismatch→refused→fix | 2.2 mastered ✅. Very curious/self-driving session — asked about api-resources short names, EndpointSlice naming, headless-vs-hardcoded-IP, read/write replica split, and independently derived the label+operator failover pattern (1.8 preview). Brief StatefulSet intro. Confidence 4/5 (highest yet). |
| 2026-08-07 | Recall (3.2 mounted-vs-env update, 2.1 CNI-broken→NotReady — both correct) + finished 1.1 RBAC (namespaced-vs-cluster probe, then full lab). | lab-1.1-rbac A–D: User/Role/RoleBinding + `can-i --as`, wrong-apiGroup break/fix (read the 403 naming `apps`), SA + real in-pod token test, ClusterRole-reused-per-ns via RoleBinding | 1.1 mastered ✅ (first Domain 01 win). Curiosity tangents: *why* apiGroups exist (groups/versioning/extensibility), full verb list. Slips: `--user` vs `--as` on can-i; "edit" not a verb; "CDI"→CNI again. SA-vs-User confusion now resolved. Confidence 4/5. ~13%→~16%. |
| 2026-08-08 | Recall (3× all correct: Service debug chain / empty-vs-refused endpoints / live-pod edit reverts) + **1.5 etcd concepts** (etcd=cluster DB, snapshot save/restore, mTLS+3 cert flags, restore-to-new-dir→repoint manifest, `minikube ssh` rationale). Short session — learner had to leave right before the lab. | none (stopped before lab Part A) | 1.5 **in progress 🟡**, not mastered (no lab, no self-test → no % change, stays ~16%). Great boundary insight: etcd snapshot ≠ volume data. mTLS was new → **requested a certificates/identity primer to open next session**. Confidence n/a. |
| 2026-08-10 | Recall (3 warm-ups: 3-port trichotomy [soft miss — swapped port↔targetPort, corrected], headless Service [ok], rollout undo --to-revision [ok]) → **Certificates/mTLS primer** (built Socratically) → **1.5 etcd lab A–D**. | lab-1.5-etcd-backup-restore FULL: snapshot save (3 certs) + etcdutl status verify, created after-backup post-snapshot, etcdutl restore→new data dir, repointed manifest hostPath, confirmed after-backup vanished. **Bonus real troubleshooting:** stale mirror-pod status (Pending vs crictl-Running) + **namespace stuck Terminating** → diagnosed via cm logs (watch at rV 118936 > restored rev 117930) → fixed by restarting controller-manager + scheduler static pods (manifest-move technique). | **1.5 mastered ✅** (2nd Domain 01 win). Deep, curiosity-driven session: derived the mTLS model himself, asked snap.db(archive) vs restored/(bootable data-dir), and the orphaned-container Q → reconcile-loop teaching. Learned the restore-aftermath control-plane restart (guaranteed exam earner). Cert/PKI gap resolved. ~16%→~19%. |
| 2026-08-11 | Recall (3× all correct: RBAC cluster-scoped-needs-ClusterRole, ConfigMap env-vs-mount [retired — owns it], etcd save-3-certs/restore-zero + etcdctl-vs-etcdutl bonus) → **5.2 Control-plane / static-pod troubleshooting** (taught Socratically, killed the "scheduler controls nodes" idea via the bootstrap chicken-and-egg). | lab-5.2-control-plane A–C FULL: **B** mystery scheduler break (bad image tag→ErrImagePull→reproduced pods-stuck-Pending→fixed by reading correct tag from another manifest→verified testpod schedules). **C** classic apiserver break (mentor-seeded bad `--etcd-servers` port 9999)→kubectl dead→diagnosed ON NODE with `crictl ps -a`+`crictl logs` (read fatal `context deadline exceeded`/etcd refused)→found port 2379 from etcd.yaml→fixed→recovered. Unscripted: spotted storage-provisioner CrashLoop, classified it as aftermath not root cause when it self-healed. | **5.2 mastered ✅** — **first Troubleshooting win (30% domain opens)**. Mostly solo, strong. Learned: static-pod fix = edit file/kubelet recreates (no apply); `crictl -a` + why; symptom→component map; post-recovery patience (brief NotReady = heartbeat). Slip: `crictl` without sudo → go-panic (needs sudo). Confidence 3/5. ~19%→~25%. |
| 2026-08-12 | **Quick 20-min recall round** (3× all correct: etcd restore 2-moves, NodePort 3-port trichotomy, `--as` vs `--user` + edit-not-a-verb — all pushed to 5d). Then **5.1 primer seed**: NotReady-node scenario. Learner didn't know node health model yet (guessed kube-controller-manager). Taught the kubelet-heartbeat/Lease model: kubelet posts heartbeat → node-controller (in kcm) is a *reader* that flips NotReady after silence; fix = `describe node` then `systemctl status kubelet` / `journalctl -u kubelet` (kubelet is a **systemd service**, not a static pod). | none (recall + concept only) | No status change (~25%). Primed 5.1 for a full break/fix next session. Spine landed: NotReady → kubelet dead/misconfigured → read its systemd logs. |
| 2026-08-12 (cont.) | Warm-up recall (1½/3: etcd-restore-restart ✅ strong; CNI NotReady/ContainerCreating ½ — blanked ContainerCreating; static-pod path ❌ said /var). Then **5.1 node troubleshooting FULL lab** — debug staircase, conditions polarity trap, heartbeat-freeze→Unknown. | lab-5.1 C+E+stretch: **C** kubelet stopped (mentor) → NotReady, diagnosed cold, fixed `enable --now`; learned active/enabled/preset. **E** corrupt `/var/lib/kubelet/config.yaml` → crash-loop, read `yaml: line 57`, fixed file (self-healed via Restart=always); learned parse≠missing, daemon-reload rule. cordon vs drain vs uncordon. | **5.1 mastered ✅** (2nd Troubleshooting win, domain now 2/5). Drove both breaks solo — strong. Slips: read "load failed" as "file not found" + proposed kubeadm-restart (corrected); `sysctl`/`systemctl` typo. Confidence 3/5 (modest). ~25%→~31%. |
| 2026-08-13 | **Short "preliminary glimpse" — learner had limited time.** Warm-up recall **3/3** (static-pod path ✅ REPAIRED /etc/kubernetes/manifests; symptom→component map ✅ + sharpened etcd-down→apiserver-down chain; aftermath-vs-root-cause ✅ + fix-one-then-recheck test). Started **1.3 lifecycle & upgrades** — framed the model (cluster = fleet of versioned binaries; upgrade = one minor at a time, CP before workers, kubelet ≤3 minors older never newer). **Two Qs posed, left unanswered** (CP-before-workers rationale; `upgrade node` vs `apply`). | none — env prep only: **`minikube node add` → 2-node cluster** ready for the drain lab. | **1.3 🟡 in progress**, no lab/self-test → **no % change, stays ~31%**. Resume next session with his answers to the 2 open Qs, then lab-1.3 Part A (drain on 2 nodes) + Part B. |
| 2026-08-15 | Warm-up **1½/3** (ContainerCreating ❌ 4th miss → gave the full stuck-status→cause table + `Node:`-field discriminator; static-pod path ❌ said `/var` again → retaught `/etc`=authored-config vs `/var/lib`=machine-state, **then RETIRED at his request** — he's confident he'll find it in the exam, and he rejected the `grep staticPodPath` derivation as more to memorize than the path itself, so don't push it; headless Service ✅). Then the requested **1.3 step-5 deep-dive**: kubelet upgrade mechanics — he nailed (a) binary-on-disk ≠ upgraded process and (b) restart via systemd cold; didn't know `apt-mark hold` at all. Taught: exact-version pinning, the **per-minor `pkgs.k8s.io` repo**, why `daemon-reload` is consistent with the 5.1 rule (the *package* edits the unit drop-in), and what the `.deb` ships vs what kubeadm writes (`/var/lib/kubelet/config.yaml` is NOT package content — retroactively explains the 5.1 break). **He requested a full apt/repos tutorial** → gave one (dpkg-vs-apt, sources.list.d, GPG keyrings, command vocabulary, `apt-cache policy`, yum mapping). Best question of the session: *"doesn't the per-minor repo make `hold` redundant?"* → the two-axes answer (magnitude vs timing; `hold` is what makes routine OS CVE patching safe). Opened **1.2** with the TLS-bootstrap chicken-and-egg. | Node exploration on `minikube ssh`: `apt-cache policy` / `dpkg -S $(which docker)` → traced the "docker installed but shows `(none)`" puzzle to a **package-name collision** (Debian's unrelated `docker` 1.5-2 vs the real `docker-ce-cli`). Confirmed kubelet is NOT dpkg-managed on minikube (raw binaries in `/var/lib/minikube/binaries/`) — which is *why* step 5 is a rehearsal here. **Env (mentor):** podman machine was stopped → `podman machine start` then `minikube start`; both nodes Ready v1.35.1. | **No status change — stays ~34%.** 1.3 step-5 thread now CLOSED (his open request from 08-14 is satisfied). **1.2 → 🟡** but barely opened: the bootstrap question was asked **3× and never answered** — he tangented into apt each time, then answered half of (a) on his way out ("the node proves possession of a secret that was itself guarded by RBAC/pre-cluster auth" — correct proof-of-possession + out-of-band chain; missing the *minimal privilege* half = `system:bootstrappers`, may only submit a CSR). **Live cliffhanger — resume there, teaching forward.** |
| 2026-08-14 | Resumed 1.3. **Env repair (mentor):** m02 was `NotReady` (`cni config uninitialized`, missing `1-k8s.conflist`) — wrote a scoped `10.244.1.0/24` bridge conflist onto m02 → both Ready. Used it as a live CNI recall (he still blanked ContainerCreating, then guessed `Pending` — re-taught the schedule→pull→network→run lifecycle). Then **lab-1.3 Part A (drain) + Part B (upgrade-order rehearsal)**. | lab-1.3 Part A FULL hands-on: create 4×`web`, cordon (predicted correctly), drain `--ignore-daemonsets --delete-emptydir-data` (all web pods → minikube, only kube-proxy DS left; empirically tested drain w/o the emptydir flag), uncordon (predicted no auto-rebalance). Part B: recited upgrade order, front-of-sequence + kubelet-package-manager split taught. | **1.3 mastered ✅** (3rd Domain 01 win). Drove Part A cold — all cordon/drain/uncordon predictions correct. Learned the drain-flags-as-waivers model + DS-vs-Deployment. Corrected: kubelet does NOT self-patch (kubeadm=CP static pods; apt/yum=kubelet binary). **Requested a step-5 deep-dive next session** (kubelet package upgrade + bootstrap → pairs with 1.2). CNI ContainerCreating still not owned (3rd miss). ~31%→~34%. |
| 2026-08-27 | Warm-up **2½/3** — much stronger than 08-26. **kubeadm upgrade order:** recovered from yesterday's hard miss; front of the sequence + `unhold`/re-`hold` cold, but drain framed as *conditional* (corrected: unconditional ritual) and reached for `enable` instead of `daemon-reload` (one hint). **etcd 3 certs:** 2/3 again — **but the 3-session mislabel was finally ROOT-CAUSED and closed** (he was conflating `etcdctl` with `etcd`; see weak-spots). **CrashLoop first-move:** named `describe`/`logs` but missed `--previous` → became the session's hook. Then **taught 5.4 in full**: two-log-streams split, the numbered-`N.log` mechanism behind `--previous`, exit codes as 128+N, `Reason` vs `Exit Code` independence, `-BackOff` = rate-limiter, and the evidence-location map. | **lab-5.4-logging A–D FULL hands-on.** **A** crasher pod → **`--previous` failed live** (`unable to retrieve container logs for docker://…`); I inspected `/var/log/pods/…` on m02 and found only `4.log` surviving → he diagnosed dead-container GC himself; re-ran fast and got the payoff (`starting` / `dying`). **B** `nginx:doesnotexist123` → read the `manifest unknown` event, derived ErrImagePull-vs-ImagePullBackOff unaided, **fixed it and verified Running**. **C** 20Mi limit vs 150M alloc → found `Reason: OOMKilled` but **`Exit Code: 1`, not 137** → unscripted lesson on Reason/ExitCode independence (OOM-killed child, PID 1 exits 1). **D** `cpu: 100` request → `Pending`, `Insufficient cpu` in Events; articulated why logs are *categorically impossible* (`Node:` empty). Cleanup: `kubectl delete ns dbg`. | **5.4 mastered ✅** — 3rd Troubleshooting win, **domain 05 now 3/5** and we crossed **40%**. Fast, high-quality, learner-driven; both of the session's best moments were **unscripted failures of my own lab script** that he reasoned through. Self-test **3/3** (scenario-framed, per the standing note that he reasons from symptoms not vocabulary — it keeps working). Slips: **`kubectl log` singular ×3**; conflated `journalctl -u kubelet` with container logs (one nudge); "decrypt" for "verify"; skipped the predict-before-running beat twice. Confidence **4/5** — *"taxing, but a strong 4."* ~37%→~43%. |
| 2026-08-26 | **11-day gap since last session.** Warm-up **1½/3**: ContainerCreating ✅ (**5th exposure, FIRST success — scenario framing was the unlock**); etcd 3-certs ½ (count + `etcdutl` ✅, but mislabeled the 3rd flag as "public key of kube api" — 2nd time; re-anchored *one to check them, two to prove yourself*); kubeadm upgrade order ❌ hard miss ("gone too long") → re-taught + reset to 1d. Then **finished 1.2** by **teaching the bootstrap forward** rather than re-asking a 4th time (per last session's tactic note — it worked). Both trust directions landed, incl. the missing *minimal-privilege* half (`system:bootstrappers` = may only create a CSR). He derived the hash mechanism unaided; corrected his motivation (**CA cert is public — the hash solves transport, not secrecy**) and anchored it on **SSH host-key fingerprints**. Unprompted sharp question: *"isn't `kubeadm upgrade apply` already updating the CP services?"* → taught **cluster-scoped-once vs per-machine-every-node**. | **lab-1.2-kubeadm-inspect A + C.** **A:** located manifests ✅; **found the certs dir himself at `/var/lib/minikube/certs`** (correct — minikube has no `/etc/kubernetes/pki`; I verified on the node and taught the standard path + the *read-the-static-pod-manifest* derivation habit); missed the kubelet config file (pointed at `/etc/kubernetes/*.conf` → taught **identity vs behavior**) and the CNI dir (→ `/etc/cni/net.d/`, where he found the very `1-k8s.conflist` I hand-wrote onto m02 on 08-14). Read the `.conf` list and connected `kubelet.conf` to the bootstrap; noticed `bootstrap-kubelet.conf` is absent (consumed). **C:** `kubeadm` not on `$PATH` → recalled his own 08-15 finding (`/var/lib/minikube/binaries/`). Ran `token list` + `token create`, then **inspected the Secrets and spotted that the 13-day `<forever>` token is simply missing the `expiration` key** — the payoff moment of the lab. | **1.2 mastered ✅** — 4th Domain 01 win; **the 3-session cliffhanger is closed.** Self-test 2.5/3 (both trust directions ✅, NotReady→CNI ✅, init sequence **omitted the CNI install step** — the #1 exam trap — while diagnosing missing-CNI correctly one question later; corrected). Slips: **"kubelet init"/"kubelet join"** for kubeadm (new, 2nd of that family); **"CDI" for CNI (4th time)**; compressed the prereq list. Fair meta-observation from him: *"we never did CNI stuff really"* — true, **1.7 is still ⬜**; drew the line on what 1.2 needs vs what 1.7 will add. ~34%→~37%. |

---

## Weighting formula (how the % is computed)

```
overall% = Σ_over_domains ( domain_weight × mastered_in_domain / total_in_domain )
domains:  01=25%/8   02=20%/6   03=15%/5   04=10%/3   05=30%/5
```
Update the **Overall** line and the progress bar every time a lesson's status changes.
The bar has 20 cells; fill `round(overall% / 5)` cells with `█`, rest `░`.
